package to

import (
	"testing"
)

func Test_to_SHA256Struct_Works_with_all_Structs(t *testing.T) {
	// This test makes sure that the same raw json put into this method returns the same SHA
	type testStruct struct {
	}
}
